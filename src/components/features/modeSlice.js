import { CreateSliceOptions, createSlice } from "@reduxjs/toolkit";



const initalState=mode2
export const modeSlice=createSlice({
    name:'mode'
    initalState,
    reducers:{
        setDarkMode:()=>{
            returnMode1;
        },
        setLightMode:()=>{
            return mode2
        }
    }
})